const path = require('path');
module.exports = {
   'seeders-path': path.resolve('src', 'seeders'),
   'migrations-path': path.resolve('src', 'migrations'),
   'config': {
      default: path.resolve('src/app/database', 'environment.ts').db,
   }
}
